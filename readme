项目目录结构规范
├── app             // 入口引导文件
    ├── const       // 全局常量定义
    ├── controller  // request请求处理中心 ——> controller ——> Response
        ├── admin   // 后台接口
        ├── api     // 前台接口
        ├── exam    // 考核相关接口
        ├── test    // 测试接口
    ├── model       // 模型层，模型、orm、验证器、自定义验证规则、请求、响应结构体数据定义
   	├── service     // 服务层，核心业务逻辑层
├── boot            // 服务启动初始化操作，清理日志，创建数据库、用户、表，初始化数据。
├── config          // 项目配置 项目配置文件简单，配置项少。
├── data            // 项目静态数据源存放目录
├── library         // 依赖库，工具库，函数封装,第三方SDK封装
    ├── response    // 全局响应封装
    ├── utils       // 公共函数、工具库
├── log             // 日志输出
    ├── sql         // 数据库执行日志
    ├── srv         // 服务日志
├── middleware      // 中间件
├── public          // 允许http外部访问的静态资源目录
├── router          // 路由定义、注册
├── main.go         // 主程序入口